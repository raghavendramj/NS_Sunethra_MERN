function templateFunction(name, city) {
  let statement = `My name is ${name} and i live in ${city}`;
  console.log(statement);
}

templateFunction("John Doe", "Germany");
templateFunction("Adam Gil", "Australia");